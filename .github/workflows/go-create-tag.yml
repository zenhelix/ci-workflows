# This file was generated using Kotlin DSL (.github/workflow-src/go-create-tag.main.kts).
# If you want to modify the workflow, please change the Kotlin file and regenerate this YAML file.
# Generated with https://github.com/typesafegithub/github-workflows-kt

name: 'Go Create Tag'
on:
  workflow_call:
    inputs:
      go-version:
        description: 'Go version to use'
        type: 'string'
        required: false
        default: '1.22'
      check-command:
        description: 'Validation command to run before tagging'
        type: 'string'
        required: false
        default: 'make test'
      default-bump:
        description: 'Default version bump type (major, minor, patch)'
        type: 'string'
        required: false
        default: 'patch'
      tag-prefix:
        description: 'Prefix for the tag'
        type: 'string'
        required: false
        default: 'v'
      release-branches:
        description: 'Comma-separated branch patterns for releases'
        type: 'string'
        required: false
        default: 'main,[0-9]+\.x'
    secrets:
      app-id:
        description: 'GitHub App ID for generating commit token'
        required: true
      app-private-key:
        description: 'GitHub App private key for generating commit token'
        required: true
jobs:
  create-tag:
    uses: 'zenhelix/ci-workflows/.github/workflows/create-tag.yml@v2'
    with:
      setup-action: 'go'
      setup-params: '{"go-version": "${{ inputs.go-version }}"}'
      check-command: '${{ inputs.check-command }}'
      default-bump: '${{ inputs.default-bump }}'
      tag-prefix: '${{ inputs.tag-prefix }}'
      release-branches: '${{ inputs.release-branches }}'
    secrets:
      app-id: '${{ secrets.app-id }}'
      app-private-key: '${{ secrets.app-private-key }}'
