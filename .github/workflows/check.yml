# This file was generated using Kotlin DSL (src/main/kotlin/workflows/base/CheckWorkflow.kt).
# If you want to modify the workflow, please change the Kotlin file and regenerate this YAML file.
# Generated with https://github.com/typesafegithub/github-workflows-kt

name: 'Check'
on:
  workflow_call:
    inputs:
      setup-action:
        description: 'Setup action to use: gradle, go, python'
        type: 'string'
        required: true
      setup-params:
        description: 'JSON object with setup parameters (e.g. {"java-version": "21"})'
        type: 'string'
        required: false
        default: '{}'
      check-command:
        description: 'Command to run for checking'
        type: 'string'
        required: true
permissions:
  contents: 'read'
concurrency:
  group: 'check-yml-${{ github.ref }}'
  cancel-in-progress: true
jobs:
  build:
    name: 'Build'
    runs-on: 'ubuntu-latest'
    timeout-minutes: 30
    steps:
    - id: 'step-0'
      name: 'Checkout'
      uses: 'actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd'
    - id: 'step-1'
      name: 'Setup Gradle'
      uses: 'zenhelix/ci-workflows/.github/actions/setup-gradle@63aedd23d8b568996497cf9210547388db43ab46'
      with:
        java-version: '${{ fromJson(inputs.setup-params).java-version || ''17'' }}'
      if: 'inputs.setup-action == ''gradle'''
    - id: 'step-2'
      name: 'Setup Go'
      uses: 'zenhelix/ci-workflows/.github/actions/setup-go@63aedd23d8b568996497cf9210547388db43ab46'
      with:
        go-version: '${{ fromJson(inputs.setup-params).go-version || ''1.22'' }}'
      if: 'inputs.setup-action == ''go'''
    - id: 'step-3'
      name: 'Setup Python'
      uses: 'zenhelix/ci-workflows/.github/actions/setup-python@63aedd23d8b568996497cf9210547388db43ab46'
      with:
        python-version: '${{ fromJson(inputs.setup-params).python-version || ''3.12'' }}'
      if: 'inputs.setup-action == ''python'''
    - id: 'step-4'
      name: 'Run check'
      run: '${{ inputs.check-command }}'
