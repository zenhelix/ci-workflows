# This file was generated using Kotlin DSL (src/main/kotlin/).
# If you want to modify the workflow, please change the Kotlin source and regenerate this YAML file.
# Generated with https://github.com/typesafegithub/github-workflows-kt

name: 'Application Release'
on:
  workflow_call:
    inputs:
      changelog-config:
        description: 'Path to changelog configuration file'
        type: 'string'
        required: false
        default: '.github/changelog-config.json'
      draft:
        description: 'Create release as draft (default true for apps)'
        type: 'boolean'
        required: false
        default: true
jobs:
  release:
    uses: 'zenhelix/ci-workflows/.github/workflows/release.yml@v2'
    with:
      changelog-config: '${{ inputs.changelog-config }}'
      draft: '${{ inputs.draft }}'
