# This file was generated using Kotlin DSL (.github/workflow-src/gradle-check.main.kts).
# If you want to modify the workflow, please change the Kotlin file and regenerate this YAML file.
# Generated with https://github.com/typesafegithub/github-workflows-kt
name: 'Gradle Check'
on:
  workflow_call:
    inputs:
      'java-version':
        description: 'JDK version to use'
        type: 'string'
        required: false
        default: '17'
      'java-versions':
        description: 'JSON array of JDK versions for matrix build (overrides java-version)'
        type: 'string'
        required: false
        default: ''
      'gradle-command':
        description: 'Gradle check command'
        type: 'string'
        required: false
        default: './gradlew check'
jobs:
  'conventional-commit':
    uses: 'zenhelix/ci-workflows/.github/workflows/conventional-commit-check.yml@v2'
  'check':
    strategy:
      matrix:
        'java-version': '${{ fromJson(inputs.java-versions || format(''["{0}"]'',
          inputs.java-version)) }}'
    uses: 'zenhelix/ci-workflows/.github/workflows/check.yml@v2'
    with:
      'setup-action': 'gradle'
      'setup-params': '{"java-version": "${{ matrix.java-version }}"}'
      'check-command': '${{ inputs.gradle-command }}'