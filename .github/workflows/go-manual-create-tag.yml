# This file was generated using Kotlin DSL (src/main/kotlin/).
# If you want to modify the workflow, please change the Kotlin source and regenerate this YAML file.
# Generated with https://github.com/typesafegithub/github-workflows-kt

name: 'Go Manual Create Tag'
on:
  workflow_call:
    inputs:
      tag-version:
        description: 'Version to tag (e.g. 1.2.3)'
        type: 'string'
        required: true
      go-version:
        description: 'Go version to use'
        type: 'string'
        required: false
        default: '1.22'
      check-command:
        description: 'Go validation command'
        type: 'string'
        required: false
        default: 'make test'
      tag-prefix:
        description: 'Prefix for the tag'
        type: 'string'
        required: false
        default: 'v'
    secrets:
      ZENHELIX_COMMITER_APP_ID:
        description: 'GitHub App ID for generating commit token'
        required: true
      ZENHELIX_COMMITER_APP_PRIVATE_KEY:
        description: 'GitHub App private key for generating commit token'
        required: true
jobs:
  manual-tag:
    uses: 'zenhelix/ci-workflows/.github/workflows/manual-create-tag.yml@v2'
    with:
      tag-version: '${{ inputs.tag-version }}'
      tag-prefix: '${{ inputs.tag-prefix }}'
      setup-action: 'go'
      setup-params: '{"go-version": "${{ inputs.go-version }}"}'
      check-command: '${{ inputs.check-command }}'
    secrets:
      ZENHELIX_COMMITER_APP_ID: '${{ secrets.ZENHELIX_COMMITER_APP_ID }}'
      ZENHELIX_COMMITER_APP_PRIVATE_KEY: '${{ secrets.ZENHELIX_COMMITER_APP_PRIVATE_KEY }}'
